# Bootstrap configuration for AWS Secrets Manager
# This file is loaded before application.properties to ensure AWS configuration is available early

# Application name
spring.application.name=memevote-backend

# AWS Configuration (only for production)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
