import { Environment } from './environment.interface';

export const environment: Environment = {
  production: true,
  apiUrl: 'https://api.memevote.com/api',
  uploadsUrl: 'https://api.memevote.com/uploads'
};
